version: 1

jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk-stretch

    steps:
      - checkout
      - run: mvn clean test -DthreadCount=5
      - store_test_results:
          path: ~/project/target
      - store_artifacts:
          path: ~/project/target